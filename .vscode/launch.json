{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Far: Run current file",
      "type": "node-terminal",
      "request": "launch",
      "command": "${config:far.compilerPath} run ${file}",
      "cwd": "${fileDirname}"
    }
  ]
}
