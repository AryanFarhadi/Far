import fs

fun main() -> i64 {
  if (fs.write_text("CON", "x") != -1) { print(1); return 1 }
  if (fs.write_text("NUL", "x") != -1) { print(2); return 2 }
  if (fs.write_text("", "x") != -1) { print(3); return 3 }
  print(0)
  return 0
}
