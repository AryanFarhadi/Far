fun dict_forin_remove() -> i64 {
  d: Dict<i64,i64> = Dict<i64,i64>()
  d.set(1,10); d.set(2,20); d.set(3,30)
  sum = 0
  for k in d.keys() {
    sum = sum + k
    if (k == 2) { d.remove(2) }
  }
  return sum
}
fun main() { print(dict_forin_remove()); return 0 }
