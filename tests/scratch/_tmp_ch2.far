fun eq(a: i64, b: i64) -> i64 { if (a != b) { return 1 } return 0 }
fun main() {
  ch = Channel<i64>(4)
  ch.send(10)
  ch.send(20)
  if (ch.recv() != 10) { print(1); return 1 }
  r = eq(ch.recv(), 20)
  print(r)
  return r
}
