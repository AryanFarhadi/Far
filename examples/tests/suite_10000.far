# suite_10000.far — AUTO-GENERATED by tools/generate_suite_10000.mjs
# 10000 assertions in 20 batches x 500 iterations
# Regenerate: node tools/generate_suite_10000.mjs
import hash
import json
import math
fun BATCH_SIZE() -> i64 { return 500 }
fun run_batch(batch_id: i64, fail_idx: i64) -> i64 {
  if (fail_idx >= 0) {
    n = batch_id * BATCH_SIZE() + fail_idx + 1
    print(n)
    return n
  }
  return 0
}
fun batch_int_add() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    a = i
    b = i * 2
    if (a + b != i * 3) { return i }
    i = i + 1
  }
  return -1
}
fun batch_int_sub() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    x = i + 100
    if (x - i != 100) { return i }
    i = i + 1
  }
  return -1
}
fun batch_int_mul() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if (i * 7 != i * 7) { return i }
    i = i + 1
  }
  return -1
}
fun batch_int_div() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    n = i + 10
    if (n / 3 != n // 3) { return i }
    i = i + 1
  }
  return -1
}
fun batch_int_mod() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
  if (i > 0) {
      if ((i * 5) % 5 != 0) { return i }
    }
    i = i + 1
  }
  return -1
}
fun batch_ipow() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
  if (i < 20) {
      if (2 ** i != math.ipow(2, i)) { return i }
    }
    i = i + 1
  }
  return -1
}
fun batch_floor_div() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    n = i + 50
    if (n // 7 != n / 7) { return i }
    i = i + 1
  }
  return -1
}
fun batch_bit_and() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if ((i & 255) != (i % 256)) { return i }
    i = i + 1
  }
  return -1
}
fun batch_bit_or() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if ((i | 0) != i) { return i }
    i = i + 1
  }
  return -1
}
fun batch_bit_xor() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if ((i ^ i) != 0) { return i }
    i = i + 1
  }
  return -1
}
fun batch_shifts() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
  if (i < 16) {
      if ((1 << i) != math.ipow(2, i)) { return i }
    }
    i = i + 1
  }
  return -1
}
fun batch_casts_i32() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if (i32(i) != i) { return i }
    i = i + 1
  }
  return -1
}
fun batch_casts_u8() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    v = u8(i)
    if (i32(v) != i % 256) { return i }
    i = i + 1
  }
  return -1
}
fun batch_bool_logic() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
  if (i == 0) {
      if (bool(0)) { return i }
    } else {
      if (!bool(1)) { return i }
    }
    i = i + 1
  }
  return -1
}
fun batch_float_round() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    f = f64(i) + 0.49
    if (int(f) != i) { return i }
    i = i + 1
  }
  return -1
}
fun batch_math_imin() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if (math.imin(i, i + 1) != i) { return i }
    i = i + 1
  }
  return -1
}
fun batch_math_imax() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    if (math.imax(i, i + 1) != i + 1) { return i }
    i = i + 1
  }
  return -1
}
fun batch_stringify_parse() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    s = json.stringify_i64(i)
    if (int(s) != i) { return i }
    i = i + 1
  }
  return -1
}
fun batch_hash_fnv() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    s = json.stringify_i64(i)
    if (hash.fnv(s) == 0) { return i }
    i = i + 1
  }
  return -1
}
fun batch_vec2_add() -> i64 {
  i = 0
  while (i < BATCH_SIZE()) {
    a = dvec2(f64(i), f64(i + 1))
    b = dvec2(1.0, 1.0)
    c = a + b
    if (int(c.x) != i + 1) { return i }
    i = i + 1
  }
  return -1
}
fun main() -> i64 {
  r = run_batch(0, batch_int_add())
  if (r != 0) { return r }
  r = run_batch(1, batch_int_sub())
  if (r != 0) { return r }
  r = run_batch(2, batch_int_mul())
  if (r != 0) { return r }
  r = run_batch(3, batch_int_div())
  if (r != 0) { return r }
  r = run_batch(4, batch_int_mod())
  if (r != 0) { return r }
  r = run_batch(5, batch_ipow())
  if (r != 0) { return r }
  r = run_batch(6, batch_floor_div())
  if (r != 0) { return r }
  r = run_batch(7, batch_bit_and())
  if (r != 0) { return r }
  r = run_batch(8, batch_bit_or())
  if (r != 0) { return r }
  r = run_batch(9, batch_bit_xor())
  if (r != 0) { return r }
  r = run_batch(10, batch_shifts())
  if (r != 0) { return r }
  r = run_batch(11, batch_casts_i32())
  if (r != 0) { return r }
  r = run_batch(12, batch_casts_u8())
  if (r != 0) { return r }
  r = run_batch(13, batch_bool_logic())
  if (r != 0) { return r }
  r = run_batch(14, batch_float_round())
  if (r != 0) { return r }
  r = run_batch(15, batch_math_imin())
  if (r != 0) { return r }
  r = run_batch(16, batch_math_imax())
  if (r != 0) { return r }
  r = run_batch(17, batch_stringify_parse())
  if (r != 0) { return r }
  r = run_batch(18, batch_hash_fnv())
  if (r != 0) { return r }
  r = run_batch(19, batch_vec2_add())
  if (r != 0) { return r }
  print(10000)
  return 0
}